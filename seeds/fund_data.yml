version: 2
seeds:
  - name: product
    config:
      column_types: 
        code: varchar(4)
        type_code: varchar(2)
  - name: type
    config:
      column_types: 
        type_code: varchar(2)